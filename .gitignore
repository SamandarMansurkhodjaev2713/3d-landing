node_modules
dist
.DS_Store
*.local
.vite

# Локальный бэкап исходной (несжатой, ~55 МБ) модели — не нужен в репозитории,
# рабочая сжатая версия лежит в public/assets/models/polus-rover.glb.
backups

# Настройки локальной среды/IDE-интеграции — не часть проекта.
.claude
